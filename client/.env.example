# Client-side env vars used at build time by Vite.
# Set this in Vercel (or your build host), NOT in the runtime backend .env.
# Must be prefixed with VITE_ to be exposed to the React app.
VITE_API_URL=https://your-backend.onrender.com
